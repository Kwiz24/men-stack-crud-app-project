<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= sneaker.name %></title>
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>
    <h1><%= sneaker.name %></h1>
    <h2><%= sneaker.size %></h2>
    <% if (sneaker.isReadyToView) { %>
        <p>This sneaker is ready to view!</p>
    <% } else { %>
        <p>This sneaker is not ready to view!</p>
    <% } %>
    <!-- Hyperlink to update size -->
    <a href="/sneakers/<%= sneaker._id %>/update/size">Update Sneaker Size</a><br>
    <!-- Hyperlink to update color -->
    <a href="/sneakers/<%= sneaker._id %>/update/color">Update Sneaker Color</a><br>
    <form action="/sneakers/<%=sneaker._id%>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= sneaker.name %></button>
    </form>
    <a href="/sneakers/<%= sneaker._id %>/edit">Edit <%= sneaker.name %></a><br>
    <a href="/sneakers">Back to Sneakers</a>
</body>
</html>
